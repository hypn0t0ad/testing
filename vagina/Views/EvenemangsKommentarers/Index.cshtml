@model IEnumerable<Systemet.Models.EvenemangsKommentarer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TidenFörKommentaren)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TidenFörKommentaren)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EvenemangsKommentarerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EvenemangsKommentarerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EvenemangsKommentarerID })
        </td>
    </tr>
}

</table>
